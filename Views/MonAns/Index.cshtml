@* @model IEnumerable<QuanLyNhaHang.Models.MonAn>

@{
    ViewData["Title"] = "Quản lý Thực đơn";
}

<style>
    .card-header-custom {
        background-color: #4e73df;
        color: white;
    }
    .table-hover tbody tr:hover {
        background-color: #f8f9fc;
    }
    .action-buttons a {
        margin: 0 4px;
    }
</style>

<div class="card shadow mb-4">
    <div class="card-header py-3 d-flex flex-row align-items-center justify-content-between card-header-custom">
        <h4 class="m-0 font-weight-bold">@ViewData["Title"]</h4>
        <a asp-action="Create" class="btn btn-light btn-sm">
            <i class="fas fa-plus"></i> Thêm món mới
        </a>
    </div>
    <div class="card-body">
        <div class="table-responsive">
            <table class="table table-bordered table-hover" width="100%" cellspacing="0">
                <thead class="thead-dark">
                    <tr>
                        <th class="text-center">Hình ảnh</th>
                        <th>Tên món ăn</th>
                        <th>Giá</th>
                        <th>Trạng thái</th>
                        <th>Danh mục</th>
                        <th class="text-center">Thao tác</th>
                    </tr>
                </thead>
                <tbody>
                    @foreach (var item in Model)
                    {
                        <tr>
                            <td class="text-center">
                                @if (item.HinhAnhMonAns.Any())
                                {
                                    <img src="~/@item.HinhAnhMonAns.First().URLHinhAnh" alt="@item.TenMonAn" class="img-thumbnail" style="width:80px; height:80px; object-fit: cover;" />
                                }
                                else
                                {
                                    <div class="img-thumbnail d-flex align-items-center justify-content-center" style="width:80px; height:80px; background-color:#e9ecef;">
                                        <i class="fas fa-image text-muted"></i>
                                    </div>
                                }
                            </td>
                            <td class="align-middle"><strong>@Html.DisplayFor(modelItem => item.TenMonAn)</strong></td>
                            <td class="align-middle">@item.Gia.ToString("N0") VNĐ</td>
                            <td class="align-middle">@Html.DisplayFor(modelItem => item.TrangThai)</td>
                            <td class="align-middle">@Html.DisplayFor(modelItem => item.MaDanhMucNavigation.TenDanhMuc)</td>
                            <td class="text-center align-middle action-buttons">
                                <a asp-action="Edit" asp-route-id="@item.MaMonAn" class="btn btn-warning btn-circle btn-sm" title="Chỉnh sửa">
                                    <i class="fas fa-pencil-alt"></i>
                                </a>
                                <a asp-action="Details" asp-route-id="@item.MaMonAn" class="btn btn-info btn-circle btn-sm" title="Xem chi tiết">
                                    <i class="fas fa-info-circle"></i>
                                </a>
                                <a asp-action="Delete" asp-route-id="@item.MaMonAn" class="btn btn-danger btn-circle btn-sm" title="Xóa">
                                    <i class="fas fa-trash"></i>
                                </a>
                            </td>
                        </tr>
                    }
                </tbody>
            </table>
        </div>
    </div>
</div> *@